/* 
 Funcion que captura el valor de un input Valido tamben para el text Area
@argument {String} id
@returns {String} valor del input capturado
 */

function valorInput_textArea(id)
{
    return document.getElementById(id).value;
}
